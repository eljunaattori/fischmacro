Setup

1) Install Python 3.x (64-bit).
2) Open a terminal in this folder.
3) Install dependencies:
   pip install -r requirements.txt
4) Run the macro:
   python macro.py

Notes:
- Keep the configs/ folder alongside macro.py.
- You may need to run the terminal as Administrator for input control.

